class A {
    var a: boolean;
    var b: boolean;
    a = true;
    b = false;
    var z: boolean;
    z = ! (!a | b) | a & ! b;
    var k: boolean;
    k = ! !a | b | a & ! b;
    print(k);
    print(z);
}