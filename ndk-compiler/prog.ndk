class A {
    var a: boolean;
    var b: boolean;
    a = true;
    b = false;
    var z: boolean;
    z = ! (!a | b) | a & ! b;
    var k: boolean;
    k = ! !a | b | a & ! b;
    print(k);
    print(z);


    var c: int;
    c = 5;
    while(c != 0) {
        c = c - 1;
        print(c);
    };



}